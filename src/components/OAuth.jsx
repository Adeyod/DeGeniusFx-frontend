const OAuth = () => {
  return (
    <div>
      <button className="w-full mt-2 bg-red-600 uppercase rounded-lg p-2 text-white font-bold h-12">
        Continue with google
      </button>
    </div>
  );
};

export default OAuth;
